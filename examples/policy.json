{
  "$schema": "https://raw.githubusercontent.com/EkuboProtocol/wallet/main/schemas/policy.schema.json",
  "version": 1,
  "chains": {
    "*": {
      "label": "Allow all actions automatically; approve only policy or simulation failures",
      "max_calls_per_batch": 4096,
      "native_value": "any_value",
      "rules": [
        {
          "effect": "allow",
          "label": "Every call, with any calldata and any native value"
        }
      ]
    }
  }
}
