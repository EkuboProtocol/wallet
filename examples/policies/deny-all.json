{
  "$schema": "https://raw.githubusercontent.com/EkuboProtocol/wallet-mcp-server/main/schemas/policy.schema.json",
  "version": 2,
  "chains": {
    "*": {
      "label": "Deny every automatic signature; each transaction needs an explicit CLI approval",
      "max_calls_per_batch": 1,
      "native": { "max_value_per_transaction": "0" },
      "targets": {},
      "approval_spenders": {},
      "tokens": {}
    }
  }
}
